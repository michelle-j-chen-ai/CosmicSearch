# ---------------------------------------------------------------------------
# FULLY PINNED to the versions installed in the deployed image
# (captured via `pip freeze` inside the built vlm-nls-search image, 2026-07-28).
# torch==2.8.0 / torchvision==0.23.0 are installed separately in the Dockerfile
# from the CPU wheel index, and lilypad_py is vendored (--no-deps), so neither
# appears below. Regenerate after any dependency change to keep builds reproducible.
# ---------------------------------------------------------------------------

# torch + torchvision are installed separately in the Dockerfile from the CPU
# wheel index (download.pytorch.org/whl/cpu) to avoid pulling CUDA wheels.
streamlit==1.60.0
transformers==4.51.3
einops==0.8.2
numpy==2.2.6
lancedb==0.30.2
pylance==4.0.0
pyarrow==24.0.0
# Roaring-bitmap decode for the fast segment-set downsample: a DORA dataset's
# membership comes back as one serialized roaring bitmap (DescribeDataSet
# include_bitmap), intersected against the corpus's dx_internal_id column.
pyroaring==1.1.0
boto3==1.43.58
botocore==1.43.58
# FastAPI frontend (web_server.py + web/) served by uvicorn.
fastapi==0.140.13
uvicorn[standard]==0.51.0
pydantic==2.13.4
# Apps Platform Postgres (exp-db) export log. Cloud Run uses the Cloud SQL
# connector + pg8000 with IAM auth; SQLAlchemy wraps the engine.
SQLAlchemy==2.0.51
cloud-sql-python-connector[pg8000]==1.21.0
pg8000==1.31.5
# Runtime deps for the VENDORED DORA proto stubs (adp/, COPYed in the
# Dockerfile). The stubs need only grpc + protobuf + the google.api annotations;
# the heavy data-explorer-py extras (requests/retrying/trino/matplotlib/simian)
# are NOT needed because dora_client avoids importing adp.public.strada.dora.
grpcio==1.83.0
protobuf==7.35.1
googleapis-common-protos==1.75.0
# Submit/poll the offline NLS scans as Lilypad workloads (nls_launcher.py). The lilypad_py
# wheel is on the AUTHENTICATED Applied (ursa) index (not public PyPI) and its declared
# deps include ray/wandb/oci (heavy, unused by the submit path) -- so it is VENDORED
# (vendor/lilypad_py-*.whl) and installed --no-deps in the Dockerfile. Here we add only the
# light import-closure the submit path needs that isn't already pinned above (boto3/grpcio/
# protobuf/pydantic are shared). Pin 2.26.0 in vendor/: the cluster speaks 2.26.0 (2.27.0
# returns UNIMPLEMENTED for the launch RPCs).
omegaconf==2.3.1
msgspec==0.21.1
aistore==1.25.0
psutil==7.2.2
click==8.4.2
rich==15.0.0
tenacity==9.1.4
tabulate==0.10.0
cryptography==49.0.0
pyOpenSSL==26.3.0
circuitbreaker==2.1.3
# CPU fast path for the int8 PCA corpus backend (gpu_corpus.py, NLS_DEVICE=cpu):
# a numba-fused int8 dot kernel reads the 12.2GB int8 corpus once per query
# (~0.25s) instead of a ~11s int8->fp32 cast + BLAS. Imported lazily.
numba==0.66.0

# ===== transitive dependencies (pinned from the deployed image) =====
GitPython==3.1.57
Jinja2==3.1.6
MarkupSafe==3.0.3
PyYAML==6.0.3
Pygments==2.20.0
aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
altair==6.2.2
annotated-doc==0.0.5
annotated-types==0.8.0
antlr4-python3-runtime==4.9.3
anyio==4.14.2
asn1crypto==1.5.1
async-timeout==5.0.1
attrs==26.1.0
blinker==1.9.0
braceexpand==0.1.7
certifi==2026.7.22
cffi==2.1.0
charset-normalizer==3.4.9
cloudpickle==3.1.2
deprecation==2.1.0
dnspython==2.8.0
exceptiongroup==1.3.1
filelock==3.29.0
frozenlist==1.8.0
fsspec==2026.4.0
gitdb==4.0.12
google-auth==2.56.2
greenlet==3.5.4
h11==0.16.0
hf-xet==1.5.2
httptools==0.8.0
huggingface_hub==0.36.2
humanize==4.16.0
idna==3.18
itsdangerous==2.2.0
jmespath==1.1.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
lance-namespace==0.9.0
lance-namespace-urllib3-client==0.9.0
llvmlite==0.48.0
markdown-it-py==4.2.0
mdurl==0.1.2
mpmath==1.3.0
multidict==6.7.1
narwhals==2.24.0
networkx==3.4.2
overrides==7.7.0
packaging==26.2
pandas==2.3.3
pillow==12.2.0
propcache==0.5.2
pyasn1==0.6.4
pyasn1_modules==0.4.2
pycparser==3.0
pydantic_core==2.46.4
pydeck==0.9.3
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
python-multipart==0.0.32
pytz==2026.3.post1
referencing==0.37.0
regex==2026.7.19
requests==2.34.2
rpds-py==0.30.0
s3transfer==0.19.2
safetensors==0.8.0
scramp==1.4.15
six==1.17.0
smmap==5.0.3
starlette==1.3.1
sympy==1.14.0
tokenizers==0.21.4
toml==0.10.2
tqdm==4.70.0
typing-inspection==0.4.2
typing_extensions==4.15.0
tzdata==2026.3
urllib3==2.7.0
uvloop==0.22.1
watchdog==6.0.0
watchfiles==1.2.0
websockets==16.1.1
xxhash==3.8.1
yarl==1.24.5
